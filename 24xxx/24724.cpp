#include <iostream>
using namespace std;

void solve(int idx) {
  int n; cin >> n;
  int a, b; cin >> a >> b;
  while (n--) {
    int u, v; cin >> u >> v;
  }

  cout << "Material Management " << idx << "\n";
  cout << "Classification ---- End!\n";
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;
  for (int i=1; i<=t; i++) solve(i);
  return 0;
}