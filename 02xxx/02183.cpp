#include <iostream>
#include <string>
using namespace std;

void solve(void) {
  int n; string s; cin >> n >> s;

  cout << s.back();
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  solve();
  return 0;
}