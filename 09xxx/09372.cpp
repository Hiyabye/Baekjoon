#include <iostream>
using namespace std;

void solve(void) {
  int n, m; cin >> n >> m;
  while (m--) {
    int a, b; cin >> a >> b;
  }
  cout << n - 1 << "\n";
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t; cin >> t;
  while (t--) solve();
  return 0;
}