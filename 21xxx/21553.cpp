#include <iostream>
#include <string>
using namespace std;

void solve(void) {
  string a; cin >> a;
  string p; cin >> p;

  cout << p;
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  solve();
  return 0;
}